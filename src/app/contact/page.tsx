import Link from 'next/link';
import { Clock, Mail, MapPin, Phone, MessageCircle } from 'lucide-react';
import { ContactForm } from '@/components/forms/ContactForm';
import { siteService } from '@/services/site.service';
export const metadata = { title: 'Contact — GUESS ENERGY + SARL', description: 'Contactez GUESS ENERGY + SARL pour vos besoins en matériel électrique, travaux, études et devis.' };
export default async function ContactPage() { const shop = await siteService.getShopInfo().catch(() => null); return <><section className="page-hero"><div className="container"><div className="breadcrumbs"><Link href="/">Accueil</Link><span>/</span><span>Contact</span></div><h1>Parlons de votre projet</h1><p>Notre équipe est à votre écoute pour vos besoins en matériel électrique.</p></div></section><section className="section"><div className="container"><div style={{ display: 'grid', gridTemplateColumns: 'minmax(240px, .8fr) minmax(0, 1.5fr)', gap: 34, alignItems: 'start' }}><div><span className="section-kicker">Nous trouver</span><h2 className="section-title" style={{ fontSize: 25, marginBottom: 24 }}>Contactez GUESS ENERGY</h2><div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>{shop?.phone && <p className="contact-line"><Phone size={19}/> <a href={`tel:${shop.phone}`}>{shop.phone}</a></p>}{shop?.email && <p className="contact-line"><Mail size={19}/> <a href={`mailto:${shop.email}`}>{shop.email}</a></p>}{(shop?.address || shop?.city) && <p className="contact-line"><MapPin size={19}/> {[shop.address, shop.city].filter(Boolean).join(', ')}</p>}</div><div className="value-card" style={{ marginTop: 28 }}><div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 10 }}><Clock size={18} style={{ color: 'var(--electric-600)' }}/><b style={{ color: 'var(--navy-900)' }}>Horaires d'ouverture</b></div><p style={{ color: 'var(--muted)', fontSize: 14, lineHeight: 1.7 }}>Lundi – Samedi : 08:00 – 18:00<br/>Dimanche : Fermé</p></div><div className="value-card" style={{ marginTop: 14 }}><div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 10 }}><MessageCircle size={18} style={{ color: '#22c15e' }}/><b style={{ color: 'var(--navy-900)' }}>WhatsApp</b></div><p style={{ color: 'var(--muted)', fontSize: 14 }}>Échangez directement avec notre équipe via WhatsApp.</p><Link href={`https://wa.me/${process.env.WHATSAPP_NUMBER?.replace(/\D/g, '') || ''}`} target="_blank" rel="noopener noreferrer" className="btn btn-sm" style={{ marginTop: 12, background: '#22c15e', color: '#fff' }}><MessageCircle size={15}/> Ouvrir WhatsApp</Link></div></div><ContactForm/></div></div></section></>; }